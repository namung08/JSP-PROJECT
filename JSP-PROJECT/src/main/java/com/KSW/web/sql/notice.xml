<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.KSW.web.dao.NotificationMapper">
    <select id="getNotificationList" resultType="com.KSW.web.dto.NotificationDTO">
        SELECT * FROM KSW_NOTIFICATION
    </select>

    <insert id="addNotification" parameterType="com.KSW.web.dto.NotificationDTO">
        INSERT INTO KSW_NOTIFICATION (NOTIFICATIONNUM, USERID, NOTIFICATIONTITLE, NOTIFICATIONDETAILS, CREATEDAT, UPDATEDAT)
        VALUES (#{notificationNum}, #{userId}, #{notificationTitle}, #{notificationDetails}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="updateNotification" parameterType="com.KSW.web.dto.NotificationDTO">
        UPDATE KSW_NOTIFICATION
        SET USERID = #{userId}, NOTIFICATIONTITLE = #{notificationTitle}, NOTIFICATIONDETAILS = #{notificationDetails}, UPDATEDAT = #{updatedAt}
        WHERE NOTIFICATIONNUM = #{notificationNum}
    </update>

    <delete id="deleteNotification" parameterType="int">
        DELETE FROM KSW_NOTIFICATION WHERE NOTIFICATIONNUM = #{notificationNum}
    </delete>
</mapper>