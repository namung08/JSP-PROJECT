<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Orderlist">
	<select id="getOrderList" resultType="orderdto" parameterType="hashmap">
		SELECT * FROM (SELECT rownum r , a.* FROM (SELECT * FROM KSW_ORDERLIST ORDER BY ORDERNUM DESC) a) b
		WHERE b.r BETWEEN#{startRow} AND #{endRow}
	</select>
	
	<select id="getOrderCnt" resultType="_int">
		SELECT COUNT(*) FROM KSW_ORDERLIST WHERE USERID=#{userid}
	</select>
	
</mapper>